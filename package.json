{
  "name": "hardhat-project",
  "description": "deploy EntryPoint, Abstract Account Factory, and test",
  "version": "0.0.2",
  "scripts": {
    "node": "npx hardhat node",
    "compile": "npx hardhat compile",
    "dev": "npx hardhat ignition deploy ./ignition/modules/EntryPoint.ts --network localhost",
    "dev:aa": "npx hardhat ignition deploy ./ignition/modules/AbstractAccount.ts --network localhost",
    "dev:aaf": "npx hardhat ignition deploy ./ignition/modules/AbstractAccountFactory.ts --network localhost",
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.24.3"
  },
  "dependencies": {
    "@account-abstraction/contracts": "0.7.0",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0"
  }
}
